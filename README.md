Server Short polling and long polling
  - mimic EC2 server creation,
  - do quick short polling calls which returns immediately
  - do long polling calls which returns only once the current status/job changes
